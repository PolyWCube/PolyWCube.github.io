name: secret

on: push
jobs:
    secrets:
        runs-on: ubuntu-latest
        env: 
            GEMINI_API_KEY_1: ${{ secrets.GEMINI_API_KEY_1 }}
        working-directory: src/base/scr
        steps:
          - uses: actions/checkout@v3
          - name: Run server
            run: node intergrative-ai.js